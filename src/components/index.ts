/**
 * Components - High-level integrated components
 * 
 * @description High-level components that combine multiple core modules
 * for simplified integration and automatic functionality.
 */

export { AudioDetectionComponent } from './AudioDetectionComponent';
export type { AudioDetectionConfig, AudioDetectionCallbacks } from './AudioDetectionComponent';